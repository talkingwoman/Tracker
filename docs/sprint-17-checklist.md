# Sprint 17 — рабочий чек-лист

Источник: Практикум, спринт 820281, итоговый урок 91d17c7a-18fd-45d2-9c38-9946d1e8700d; ТЗ https://github.com/Yandex-Practicum/iOS-TrackerApp-Public.
Работа в sprint_17. Коммиты, push и отправку на ревью не выполнять без отдельного запроса.

## План реализации и проверок

1. Снять и повторно проверить эталоны светлой темы до изменений цветов.
   - Семь эталонов сохранены, повторный запуск 16.09.2026 прошёл.
   - Тест контраста основной кнопки воспроизводит дефект тёмной темы.
2. Добавить адаптивные цвета, сохранить светлую тему, добавить тёмные эталоны.
3. Выделить чистую фильтрацию `visibleCategories`: расписание, поиск без учёта регистра, выполненные/невыполненные на выбранный день. Покрыть сочетания тестами.
4. Добавить модальный экран фильтров. Все/сегодня сбрасывают фильтр, сегодня меняет дату; галочка только у выполненных/невыполненных; фильтр сохраняется при смене даты. Кнопка скрыта только при отсутствии запланированных трекеров, а не при пустом результате фильтра. Нижний inset не даёт кнопке закрывать карточки.
5. Контекстное меню: редактирование с предзаполнением и сохранением UUID/истории; удаление с подтверждением и очисткой записей. Дополнительно закрепление без изменения исходной категории, отдельная виртуальная секция.
6. Статистика: общее число завершений из записей Core Data, пустой экран при нуле, обновления после отметок/удалений. Проверить перезапуск на отдельной тестовой базе.
7. AppMetrica через SPM, отдельный сервис. Main: open/close без item, click с add_track/track/filter/edit/delete. Проверить структуру событий через тестовый приёмник. Для реальной отправки нужен API key пользователя.
8. Довести локализации ru/en/de/ar, plurals, генерацию типизированных ресурсов по дополнительной задаче. Проверить ключи и сборку с чистого состояния.
9. Проверить сборку, unit/snapshot-тесты, ручные сценарии и доступные макеты. Обновление эталонов только после осмотра ожидаемых изменений.

## Реализовано и проверено 16.09.2026

- Прочитаны доступные учебные уроки: онбординг, локализация (7), скриншот-тесты (3), тёмная тема (2), аналитика (4), поиск, код-ревью (4), итоговая задача. Закрыт только финальный урок обратной связи.
- Фильтры, сочетания с датой и поиском, сброс «Все»/«Сегодня», разделение по категориям реализованы. Контроллерный тест проверяет сохранение фильтра при смене даты и видимость кнопки при пустом результате.
- Редактирование, удаление с подтверждением, закрепление/открепление реализованы. Проверены сохранение UUID и истории при изменении, очистка истории при удалении, исходная категория при закреплении.
- Статистика получает записи через отдельный протокол и обновляется при изменении Core Data. Проверены отображение после отметки и возврат к заглушке после удаления.
- Исходная модель Core Data оставлена для миграции. В V2 добавлен `isPinned` со значением false по умолчанию. Повторное открытие отдельной SQLite после миграции сохраняет данные.
- AppMetricaCore подключена через SPM. Параметры всех семи вариантов событий проверены тестовым приёмником; реальная отправка ожидает ключ.
- Ru/en/de/ar, `.stringsdict`, R.swift code generation. Проверены 30 случаев склонений и равенство наборов ключей четырёх локализаций; отдельный XCTest проверяет типизированные вызовы.
- 33 теста, включая 19 скриншотов. Светлые эталоны сначала сняты и проверены до изменения темы. Намеренная замена фона на розовый вызвала ожидаемое падение (`tracker-sprint17-mutation-red.log`), затем изменение удалено. Режим перезаписи эталонов в коде не оставлен.
- Независимое статическое ревью выявило открытие обычной БД из тестов; устранено: модель загружается напрямую из bundle, SceneDelegate под XCTest не инициализирует пользовательское хранилище.
- Через интерфейс исходного Figma проверены тёмный главный экран, фильтры, статистика; заглушка поиска экспортирована из слоя `1:1349` в масштабах 1x/2x/3x. Статистика: 343×90, inset 16, радиус 16, граница 1, внутренние отступы 12 и 7. Системные элементы UIKit могут отличаться от старой версии iOS в макете.
- Коммиты и push не выполнялись. Инструкции запуска и оставшихся проверок: `sprint-17-run.md`.
- Итоговый прогон 16.09.2026 20:01:59: **33 теста, 0 ошибок**, `TEST SUCCEEDED`; `/tmp/tracker-sprint17-verified.log`. `git diff --check` без замечаний. Скрипт локализации прошёл для четырёх языков.
- Обычный запуск выполнен через simctl (20:02, bundle `ru.yandex.practicum.Tracker`). Финальный ручной осмотр остановлен: Mac заблокирован, интерфейс симулятора недоступен до ручной разблокировки пользователем. Полный ручной сценарий после последней сборки пока не подтверждён.

## Внешние шаги и границы готовности

- API key AppMetrica подключён через локальный `Tracker/App/AnalyticsKeys.plist`, исключённый из Git. 16.09.2026 подтверждена отправка SDK и появление событий в кабинете (подробности ниже).
- Отдельный EmojiMixer исключён по прямому указанию пользователя; упражнения для него не выполняются.
- Figma MCP: исходный файл не даёт edit-доступа; копия достигла лимита Starter. Использован доступный интерфейс Figma. Полное попиксельное совпадение всех состояний на всех iPhone не заявляется.
- Код-ревью статистики с одногруппником нельзя заменить самопроверкой; нужен напарник и его PR.
- Урок «Обратная связь» пока закрыт после формы сдачи; работу автоматически не сдаём.
- Дополнительные показатели статистики (лучший период, идеальные дни, среднее) и аналитика других экранов не реализованы: это отдельные необязательные пункты. Выполненные дополнительные пункты — закрепление, локализация других экранов, два дополнительных языка, R.swift и скриншоты других экранов.

## Финальная проверка 16.09.2026, 22:48–23:10

- В разблокированном симуляторе создана категория `QA` и привычка `QA Sprint 17` с расписанием вторник/среда, эмоджи и цветом. Проверены доступность создания только после заполнения обязательных полей и единственное выделение эмоджи/цвета.
- Отметки 15 и 16 сентября увеличили счётчик до двух. Фильтр невыполненных скрывает отмеченный трекер, сохраняется при смене даты и не скрывает кнопку фильтров при пустом результате. Фильтр выполненных показывает карточку с двумя отметками.
- Для будущей среды, 23 сентября, карточка отображается, а кнопка отметки недоступна. Поиск `qa sprint` находит карточку без учёта регистра; несуществующий запрос показывает заглушку, отмена поиска восстанавливает список.
- Пользователь помог открыть контекстное меню долгим нажатием и прокрутить форму (эти жесты не срабатывали через инструмент управления). Форма редактирования предзаполнила название, категорию, расписание и счётчик. Изменены название на `QA Sprint 17 edited`, эмоджи на сердце и цвет на зелёный; две отметки сохранились.
- Перезапуск обычного приложения сохранил изменённую карточку и обе отметки. Проверены английский светлый и русский тёмный главный экран, календарь и статистика. Снятие сегодняшней отметки уменьшило счётчик и статистику с двух до одного, склонения «2 дня» / «1 день» корректны.
- AppMetrica: ключ в кабинете соответствует локальному; SDK подтвердил `Client event is sent` для `open` в 23:04:27. В отчёте за сегодня затем появились 7 событий `click` от одного устройства. До этого недельный отчёт показывал пустой результат и ошибку загрузки; подтверждена именно доставка в отчёт, а не только локальная запись Analytics. Временное SDK-логирование удалено из исходника после диагностики.
- Итоговая сборка без диагностической правки: `TEST BUILD SUCCEEDED`. Повторный полный прогон в 23:07:51: **33 теста, 0 ошибок**, `TEST EXECUTE SUCCEEDED`, лог `/tmp/tracker-sprint17-final-tests.log`. Обычная сборка установлена обратно после тестов.
- Ручное подтверждение удаления и закрепления ещё не выполнено; автоматические тесты этих сценариев прошли. Для удаления тестовой карточки требуется отдельное подтверждение пользователя в момент удаления. Тестовые `QA` и `QA Sprint 17 edited` пока оставлены в симуляторе (одна отметка за 15 сентября).
- Коммиты, push и PR не выполнялись.
